# ツールディレクトリへのアクセス制限
# 本番環境では、このディレクトリへのアクセスを制限することを推奨します

# すべてのアクセスを拒否（必要に応じてコメントアウト）
# Deny from all

# または、特定のIPアドレスからのみアクセスを許可
# Allow from 127.0.0.1
# Allow from YOUR_IP_ADDRESS
# Deny from all

# 注意: 本番環境では、このディレクトリへのアクセスを完全に拒否するか、
# 認証を追加することを強く推奨します

